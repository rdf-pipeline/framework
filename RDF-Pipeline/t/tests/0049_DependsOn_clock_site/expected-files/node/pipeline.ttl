# Cause the updater to be fired on every request, because
# the node dependsOn a clock page.

@prefix p: <http://purl.org/pipeline/ont#> .
@prefix : <http://localhost/node/> .

:hello a p:FileNode ;
  # This URL one seems to be unreliable (500 errors often):
  # p:dependsOn <http://tycho.usno.navy.mil/cgi-bin/timer.pl> .
  # Maybe this URL will be more reliable:
  p:dependsOn <http://www.usno.navy.mil/> .
